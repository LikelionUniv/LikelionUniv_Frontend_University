import { Login } from '../components/login/Login';
import '../components/LoadScript'

const LoginPage = () => {
    return (
        <>
            <Login />
        </>
    );
};

export default LoginPage;
